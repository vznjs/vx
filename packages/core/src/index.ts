// The nxt engine.
//
// This is the package that loads workspace + project configs, builds the task
// graph, schedules execution, and owns the cache. It is intentionally empty at
// this stage — the public API is being designed alongside the config schema.

export const VERSION = '0.0.0'
