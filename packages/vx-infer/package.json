{
  "name": "@vzn/vx-infer",
  "version": "0.0.0",
  "description": "Inferred tasks for vx — scripts(), vite(), vitest(), next(), tsc(): a package with no vx.config gets its tasks from the tools it uses, with cache blocks that fit them; a task the package declares itself always wins.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "index.ts",
    "src",
    "README.md"
  ],
  "engines": {
    "bun": ">=1.4"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@vzn/vx": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "@vzn/vx": "*"
  }
}
