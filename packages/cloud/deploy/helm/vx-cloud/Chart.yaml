apiVersion: v2
name: vx-cloud
description: >-
  The vx orchestrator service — a coordinator that holds the global ready
  queue and a horizontally-scaled, fungible worker fleet. k8s owns pod
  lifecycle; vx owns which ready task each worker pulls.
type: application
# Chart version — bump on any template/values change.
version: 0.1.0
# The @vzn/vx-cloud release this chart's default image tag tracks.
appVersion: "0.0.0"
keywords:
  - vx
  - task-runner
  - monorepo
  - build-cache
  - ci
home: https://github.com/vznjs/run
sources:
  - https://github.com/vznjs/run
maintainers:
  - name: vzn
