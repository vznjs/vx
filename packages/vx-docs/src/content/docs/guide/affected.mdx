---
title: 'Only what changed'
description: 'From a changed file to the packages it touches and their dependents, and what a change cannot be traced to.'
chapter: 7
problem: 'CI builds all four packages for a README edit.'
---

{/* TODO(R2): the chapter. What it teaches: From a changed file to the packages it touches, then to their dependents; `--affected`; what a change cannot be traced to (a root file no input names). Widget: GraphExplorer (change mode). */}

Someone fixed a typo in a README, and CI built all four packages. This chapter shows how a tool finds the few tasks a change can reach.
