---
title: 'Tasks'
description: 'A task is one command, in one package, that reads inputs and writes outputs, and why each task is one command.'
chapter: 2
problem: 'The loop runs "scripts"; what exactly is one unit of work?'
---

{/* TODO(R2): the chapter. What it teaches: A task: one command, in one package, that reads inputs and writes outputs. Why one command per task (so each can be ordered, skipped, cached). `package#task` names. Widget: none. */}

The loop runs "scripts", but a script can do anything. Before a tool can order, skip or reuse work, it needs a unit of work it can name. This chapter defines one.
