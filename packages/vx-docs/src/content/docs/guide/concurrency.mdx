---
title: 'Concurrency'
description: 'Running independent tasks at once: workers, the critical path, and why the order ready tasks start in changes the finish time.'
chapter: 4
problem: 'The graph is right but the run takes as long as the loop.'
---

{/* TODO(R2): the chapter. What it teaches: Independent tasks run at once; workers; the critical path; why the choice of which ready task starts first changes the finish time. Widget: SchedulerSim. */}

The graph puts every task in the right order, but the run still takes as long as the loop did, because it still runs one task at a time. This chapter shows how to run more at once.
