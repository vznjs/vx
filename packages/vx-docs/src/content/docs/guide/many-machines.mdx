---
title: 'Many machines'
description: 'Sharing results between machines with a remote cache, and running tasks elsewhere with remote execution.'
chapter: 8
problem: 'Your laptop and CI build the same thing twice.'
---

{/* TODO(R2): the chapter. What it teaches: A shared remote cache (and why only trusted writers push); remote execution, where the worker holds only the declared inputs; what each costs. Diagram: one static diagram (build-time SVG, components/guide/diagram). */}

Your laptop built `utils` this morning, and CI built the same `utils` an hour later. This chapter shows how machines share their work.
