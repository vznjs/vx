---
title: 'How vx is built'
description: 'The pipeline inside vx, a seam at each stage, the local floor, and a plugin in twenty lines.'
chapter: 9
problem: 'You now know the ideas; how does one tool hold them without growing a branch for every vendor?'
---

{/* TODO(R2): the chapter. What it teaches: The pipeline (config, project, graph, key, schedule, executor, cache, telemetry) with a seam at each stage; the local floor; a plugin in 20 lines. Widget: PipelineExplorer. */}

You now know the ideas: tasks, a graph, workers, keys, a sandbox, a remote. This chapter shows how vx holds all of them without a special case for every vendor.
