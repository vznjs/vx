node_modules
dist
# packages/cloud/ui/dist is the prebuilt single-file dashboard the cloud binary
# embeds via `with { type: 'file' }`. Committing it means `bun build --compile`
# works straight out of a fresh checkout — no SPA build step required to produce
# the binary.
!packages/cloud/ui/dist/
*.tsbuildinfo
coverage
.DS_Store
.env
.env.local
.idea
.vscode
*.log
.vx
.nxt
.claude/worktrees/
.nx
.turbo
*.cpuprofile
*.heapsnapshot
